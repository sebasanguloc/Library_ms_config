server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistry: false
    registryWithEureka: false
    serviceUrl:
      defaultZone: http://${eureka.hostname}:${server.port}/eureka/
